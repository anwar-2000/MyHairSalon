import React from 'react'
import classes from "@styles/salonsside.module.css"
interface Props {
    salons : any[]
}

const SalonsSide:React.FC<Props> = ({salons}) => {
  return <div></div>
}

export default SalonsSide